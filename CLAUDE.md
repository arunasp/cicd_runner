# Notes for Claude

This file points at the same guidance as [`AGENTS.md`](AGENTS.md); it exists
so that the instructions are found regardless of which convention a given
tool looks for.

**Read [`AGENTS.md`](AGENTS.md).** It lists the skills this repository ships,
when to load each, and the ground rules for working here.

If you have the `docker-cicd-runner` skill installed in your account, that is
the same content as `skills/opencode/docker-cicd-runner/` in this repository —
`make skills-check` enforces that they are byte-identical.
